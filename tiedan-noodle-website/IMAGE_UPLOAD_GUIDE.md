# 图片上传和菜单修改指南

## 📸 如何上传图片

### 方法一：使用 public 文件夹（推荐）

1. **创建图片目录结构**
   ```bash
   cd tiedan-noodle-website
   mkdir -p public/images/menu
   mkdir -p public/images/store
   ```

2. **上传图片到对应文件夹**
   - 菜单图片：放到 `public/images/menu/` 文件夹
   - 店铺图片：放到 `public/images/store/` 文件夹

3. **图片命名规范**
   - 菜单图片示例：
     - `chicken-noodle-classic.jpg` (经典鸡汤刀削面)
     - `chicken-noodle-spicy.jpg` (麻辣鸡汤刀削面)
     - `side-cucumber.jpg` (凉拌黄瓜)
   - 店铺图片示例：
     - `storefront.jpg` (店面外观)
     - `interior-1.jpg` (店内环境)
     - `kitchen.jpg` (厨房)

4. **图片要求**
   - 格式：JPG, PNG, WebP
   - 推荐尺寸：
     - 菜单图片：800x600px (4:3 比例)
     - 店铺图片：1200x900px 或更大
   - 文件大小：建议每张图片 < 500KB

### 方法二：使用外部图片链接

如果你的图片已经上传到其他地方（如图床、CDN），可以直接使用完整的 URL：

```javascript
image: 'https://your-cdn.com/images/noodle.jpg'
```

---

## 🍜 如何修改菜单

### 1. 打开菜单数据文件

文件位置：`tiedan-noodle-website/src/constants/menuData.js`

### 2. 修改现有菜品

找到要修改的菜品，更改相应字段：

```javascript
{
  id: 'noodle-1',              // 唯一ID，不要修改
  name: '经典鸡汤刀削面',        // 菜品名称
  description: '精选老母鸡...',  // 菜品描述
  price: 28,                    // 价格（数字）
  category: 'noodles',          // 分类：noodles/sides/drinks
  image: '/images/menu/chicken-noodle-classic.jpg',  // 图片路径
  isSignature: true,            // 是否为招牌菜
  available: true               // 是否可售
}
```

### 3. 添加新菜品

在对应分类的数组中添加新对象：

```javascript
// 在刀削面系列中添加
{
  id: 'noodle-7',                    // 新的唯一ID
  name: '香菇鸡汤刀削面',             // 新菜品名称
  description: '香菇与鸡汤的完美结合', // 描述
  price: 30,                         // 价格
  category: 'noodles',               // 分类
  image: '/images/menu/noodle-mushroom.jpg',  // 图片路径
  isSignature: false,                // 不是招牌菜
  available: true                    // 可售
}
```

### 4. 删除菜品

有两种方式：

**方式1：设置为不可售（推荐）**
```javascript
{
  id: 'noodle-3',
  name: '番茄鸡汤刀削面',
  // ... 其他字段
  available: false  // 改为 false，菜品不会显示
}
```

**方式2：直接删除整个对象**
```javascript
// 删除整个 { ... } 对象
```

### 5. 修改分类

如果要添加新分类或修改分类名称，找到 `categories` 数组：

```javascript
export const categories = [
  {
    id: 'noodles',              // 分类ID
    name: '刀削面系列',          // 分类名称
    description: '精选老母鸡...' // 分类描述
  },
  // 添加新分类
  {
    id: 'desserts',             // 新分类ID
    name: '甜品',               // 新分类名称
    description: '精选甜品'      // 新分类描述
  }
];
```

---

## 📝 完整示例

### 示例1：添加一个新的刀削面

```javascript
// 在 menuItems 数组的刀削面部分添加：
{
  id: 'noodle-7',
  name: '三鲜鸡汤刀削面',
  description: '虾仁、鱿鱼、蛤蜊配鸡汤刀削面，海鲜鲜美',
  price: 38,
  category: 'noodles',
  image: '/images/menu/noodle-seafood.jpg',
  isSignature: false,
  available: true
}
```

### 示例2：修改价格

```javascript
// 找到对应菜品，修改 price 字段
{
  id: 'noodle-1',
  name: '经典鸡汤刀削面',
  description: '精选老母鸡熬制高汤，配以手工刀削面，汤鲜面劲道',
  price: 30,  // 从 28 改为 30
  // ... 其他字段不变
}
```

### 示例3：设置招牌菜

```javascript
{
  id: 'noodle-4',
  name: '菌菇鸡汤刀削面',
  // ... 其他字段
  isSignature: true,  // 从 false 改为 true，成为招牌菜
  available: true
}
```

---

## 🖼️ 图片路径说明

### 本地图片路径格式

```javascript
// 正确的路径格式（从 public 文件夹开始）
image: '/images/menu/chicken-noodle-classic.jpg'

// 错误的路径格式
image: 'public/images/menu/...'  // ❌ 不要包含 public
image: './images/menu/...'       // ❌ 不要使用相对路径
```

### 外部图片链接格式

```javascript
// 使用完整的 URL
image: 'https://example.com/images/noodle.jpg'
image: 'https://cdn.yoursite.com/menu/dish1.jpg'
```

---

## 🔄 修改后如何查看效果

1. **保存文件**
   - 保存 `menuData.js` 文件

2. **刷新浏览器**
   - 如果开发服务器正在运行，浏览器会自动刷新
   - 如果没有自动刷新，手动刷新页面（F5 或 Ctrl+R）

3. **查看菜单页面**
   - 访问：http://localhost:5173/menu
   - 检查新添加或修改的菜品是否正确显示

---

## ⚠️ 注意事项

1. **ID 必须唯一**
   - 每个菜品的 `id` 必须是唯一的
   - 建议使用格式：`分类-数字`（如 `noodle-7`, `side-8`）

2. **分类必须匹配**
   - `category` 字段必须是 `categories` 数组中定义的 ID
   - 可选值：`noodles`, `sides`, `drinks`

3. **价格使用数字**
   - `price: 28` ✅ 正确
   - `price: "28"` ❌ 错误（不要用引号）

4. **图片文件名**
   - 使用英文和数字
   - 使用连字符 `-` 而不是空格
   - 小写字母
   - 示例：`chicken-noodle-spicy.jpg` ✅

5. **保存后检查**
   - 修改后保存文件
   - 检查浏览器控制台是否有错误
   - 如果有语法错误，页面可能无法加载

---

## 🛠️ 常见问题

### Q: 图片不显示怎么办？

A: 检查以下几点：
1. 图片文件是否在 `public/images/menu/` 文件夹中
2. 图片路径是否正确（注意大小写）
3. 图片文件名是否与代码中的路径匹配
4. 浏览器控制台是否有 404 错误

### Q: 修改后没有生效？

A: 尝试以下步骤：
1. 确认文件已保存
2. 刷新浏览器（Ctrl+F5 强制刷新）
3. 检查是否有 JavaScript 语法错误
4. 重启开发服务器

### Q: 如何批量上传图片？

A: 
1. 将所有图片放到 `public/images/menu/` 文件夹
2. 确保文件名与 `menuData.js` 中的路径匹配
3. 可以使用文件管理器直接拖拽多个文件

---

## 📞 需要帮助？

如果遇到问题，可以：
1. 检查浏览器控制台的错误信息
2. 确认 JSON 语法是否正确（逗号、括号等）
3. 参考现有菜品的格式进行修改

---

**提示**：修改菜单数据后，建议先在开发环境测试，确认无误后再部署到生产环境。
